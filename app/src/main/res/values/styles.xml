<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
        <item name="android:textColorSecondary">@color/secondaryTextColor</item>
<!--        <item name="android:icon">@color/secondaryTextColor</item>-->
        <item name="android:selectableItemBackground">@color/rv_item_color_state</item>
        <item name="android:windowBackground">@color/primaryLightColor</item>
        <item name="cardBackgroundColor">@android:color/white</item>
<!--        <item name="android:popupMenuStyle">@style/ShowTracker.PopupMenu</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

<!--    <style name="ShowTracker.PopupMenu" parent="android:Widget.Material.ListPopupWindow">-->
<!--        <item name="android:popupBackground">@color/selectedItemColor</item>-->
<!--    </style>-->

</resources>
